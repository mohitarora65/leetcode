# LeetCode

Daily LeetCode solutions.

## Structure

Each solution lives under `<category>/NNNN-problem-slug/`, e.g. `arrays/0001-two-sum/`, containing the solution file and a short `notes.md` with approach and complexity.
